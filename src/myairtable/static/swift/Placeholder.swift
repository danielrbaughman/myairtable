// Static runtime for MyAirtable Swift target.
// Individual runtime files (AirtableClient, AirtableError, OrmTable, etc.) will
// be added in F2 (Swift runtime core). This placeholder exists so the
// `MyAirtableStatic` SPM library target has at least one source file.

public enum MyAirtableStatic {
    public static let version = "0.0.1-dev"
}
